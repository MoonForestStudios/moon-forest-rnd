extends Node2D

var text: String = "Hola, Godot 4."

#func _ready():
	#print(text+"\n")
	#
	#var name = "Pipe"
	##var say = "Hola, "+name
	#var say = "Hola, %s" % name
	#var sentence = "El nombre de mi amigo es %s y mi hermano %s" % ["Juan", "Diego"]
	#
	#var length = sentence.length()
	#var value = "Godot"
	#var other = "  gOdot  "
	#var otherSay = "Duplicate say"
	#var findText = "say"
	#
	#print(sentence, "\nLa longitud es: %d" % length)
	#print("Texto is empty: %s" % text.is_empty())
	#print(value.to_upper())
	#print(value.to_lower())
	#print(other.strip_edges())
	#print(otherSay.replace("say", "the say"))
	#print(otherSay.find(findText))
	#print("La palabra esta dentro del texto: %s" % otherSay.contains(findText))
